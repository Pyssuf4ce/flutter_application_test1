name: flutter_application_test1
description: "A new Flutter project."


version: 1.0.0+1

environment:
  sdk: ^3.11.1


dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2  # ต้องมีบรรทัดนี้!
  supabase_flutter: ^2.5.0 # ตัวเชื่อม Supabase
  image_picker: ^1.1.2    # ตัวเลือกรูปจากมือถือ
  cupertino_icons: ^1.0.8
  shimmer: ^3.0.0
  intl: ^0.20.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.9

flutter:


  uses-material-design: true
  
