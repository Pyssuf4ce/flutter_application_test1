import 'package:flutter/material.dart';

/// สีกลางของแอป VAULT — ใช้ไฟล์นี้แทน hardcode สีตรงๆ ในแต่ละหน้า
class AppColors {
  AppColors._();

  static const accent = Color(0xFF35408B);
  static const primary = Color(0xFF4D58A5);
  static const background = Color(0xFFF8F9FA);
  static const textDark = Color(0xFF191C1D);
  static const textMuted = Color(0xFF767682);
  static const textSubtle = Color(0xFF586062);
  static const border = Color(0xFFE2E9EC);
  static const inputFill = Color(0xFFF5F7FA);
  static const divider = Color(0xFFF1F3F4);
  static const accentLight = Color(0xFFD1DDFA);
  static const error = Color(0xFFba1a1a);
}
